# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Drift runtime state (per-project build cache) and generated outputs
.drift/
out/

# API runtime data (durable queue + project content)
data/

# Node (dashboard)
node_modules/

# Vercel proxy config (contains the backend host — local-only, never commit the IP)
apps/web/vercel.json

# Editors / OS
.vscode/
.idea/
.DS_Store
